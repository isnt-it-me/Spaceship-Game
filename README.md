# 🚀 Spaceship Shooter

A retro-style arcade space shooter built using Python. Defend the galaxy against waves of alien invaders!

## ⚠️ Important Setup Instructions

Before running the game, you must ensure you have the library installed and the images in the right place.

### 1. Install Pygame
This game requires the `pygame` library. Open your terminal or command prompt and run:
```bash
pip install pygame
